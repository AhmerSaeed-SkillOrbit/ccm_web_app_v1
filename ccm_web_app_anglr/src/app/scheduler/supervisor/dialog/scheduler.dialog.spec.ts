import {
  ComponentFixture,
  inject,
  TestBed
} from '@angular/core/testing';
import { SchedulerDialogComponent } from './scheduler.dialog'
import { ScheduleService } from '../../core/services/schedule/schedule.service'
import { HttpService } from '../../core/services/base/http.service';

fdescribe('MatDialog', () => {
  
  let component: SchedulerDialogComponent;
  //let scheduleService: MockSchedulerService;
  

  beforeEach(() => { 
    //scheduleService = new MockSchedulerService();
    //component = new LoginComponent(service);
  });

});
